# Docker syslog szerver

docker compose up -d
docker ps

Teszt ```logger "DEBIAN SYSLOG TESZT"```
Powershell script

```powershell
	Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass .\test_syslog.ps1
```
